; run this example: cat example | ./lisprima | node
(const foo
       {:bar ["bam"
              "boom"]
        :qux {:xyzzy "abc"
              :plink ["plank"
                      "plunk"]}})

(.log console foo)
